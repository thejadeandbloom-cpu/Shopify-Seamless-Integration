{
  "version": 2,
  "buildCommand": "npm i -g pnpm@latest && pnpm config set audit false && pnpm install --no-frozen-lockfile && pnpm run build",
  "installCommand": "npm i -g pnpm@latest && pnpm config set audit false && pnpm install --no-frozen-lockfile",
  "outputDirectory": "artifacts/jade-bloom/dist/public"
}
