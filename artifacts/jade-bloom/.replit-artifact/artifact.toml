kind = "web"
previewPath = "/"
title = "Jade and Bloom"
version = "1.0.0"
id = "artifacts/jade-bloom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/jade-bloom run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jade-bloom", "run", "build" ]
publicDir = "artifacts/jade-bloom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
